#version 430 core

// dumb optimization attempt:
// 1) we want to render to a voxel grid 
// 2) as projected through a 2D plane,
// 3) with resolution such that each voxel corresponds to a unique pixel

void main()
{
}
